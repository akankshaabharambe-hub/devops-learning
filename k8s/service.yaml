apiVersion: v1
kind: Service
metadata:
  name: platform-delivery-demo-service
spec:
  selector:
    app: platform-delivery-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
